{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1 class="text-center pt-3 pb-3">Catégorie {{ category.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="p-3" href="{{ path('category_index') }}"><i class="fa fa-reply"></i> Retour</a>

    <a class="p-3" href="{{ path('category_edit', {'id': category.id}) }}"><i class="fa fa-edit"></i> Modifier</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
