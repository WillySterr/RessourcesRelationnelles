{% extends 'base.html.twig' %}

{% block title %}Editer Articles{% endblock %}

{% block body %}
    <a class="btn btn-primary" href="{{ path('articles_index') }}">Retour</a>
<div class="global-container">
<div>
        {# {% for photo in photo.image %} #}
        <div>
                <h6>Ancienne images</h6>
            <img src="{{ asset('/uploads/'~article.photo) }}" alt="ancienne image" width="400">
        </div>
           
        <div class="mt-2">
         <h6>Ancienne vidéo</h6>
            <video controls width="400"><source src="{{ asset('/uploads/'~article.video) }}" type="video/mp4"></video>
        </div>
        {# {% endfor %} #}
    </div>
    {{ include('articles/_form.html.twig', {'button_label': 'Update'}) }}
</div>

    {{ include('articles/_delete_form.html.twig') }}

{% endblock %}
